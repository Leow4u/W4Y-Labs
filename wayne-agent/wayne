#!/usr/bin/env python3
"""
Wayne Agent CLI launcher.

This wrapper should behave like the installed `wayne` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from wayne_cli.main import main
    main()
